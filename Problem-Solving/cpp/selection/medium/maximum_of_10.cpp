#include <iostream>
using namespace std;

int main()
{
    cout << "===========================\n";

    while (1)
    {
        int maxi, num;
        cin >> maxi;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cin >> num;
        if (maxi < num)
            maxi = num;
        cout << maxi;
    }

    cout << "\n===========================\n";
    return 0;
}
