
  void print_reversed() {
    for (Node *cur = tail; cur; cur = cur->prev) {
      cout << cur->data << " ";
    }
    cout << "\n";
  }

  void print_reversed() {
    Node *cur = tail;
    while (cur) {
      cout << cur->data << " ";
      cur = cur->prev;
    }
    cout << "\n";
  }
